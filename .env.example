# 本檔僅供說明，不需要建立 .env 檔案
#
# STORYBOOK_BASIC_AUTH 設在 Netlify 後台 env var，不進 repo：
#   Netlify → Site configuration → Environment variables → Add a variable
#     Key:   STORYBOOK_BASIC_AUTH
#     Value: your_user:your_password   （多組空格分隔："alice:pw1 bob:pw2"）
#
# 詳細流程執行：npm run setup:netlify
