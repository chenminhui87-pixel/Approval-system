{
  "defaultMode": "auto",
  "_comment_plugin_setup": "First-time setup（本機 Claude Code）: run `/plugin marketplace add github:ajenchen/design-system` → then `/plugin install design-system@qijenchen-ds` → restart。網頁/雲端環境 `/plugin` 不可用，需在本機才裝得到完整 governance。Bootstrap hooks（committed in repo，不依賴 plugin）在 plugin 裝好前先補 first-time mechanical gate。詳 CLAUDE.md。",
  "permissions": {
    "allow": [
      "Bash(npm install)",
      "Bash(npm ci)",
      "Bash(npm run *)",
      "Bash(npx *)",
      "Bash(git *)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/check_plugin_bootstrap.sh\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/block_production_edit_without_plugin.sh\""
          }
        ]
      }
    ]
  }
}
